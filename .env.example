# Copy to `.env` and fill in your ROG machine's LAN IP.
# ROG must be running: `ollama serve` with OLLAMA_HOST=0.0.0.0:11434
# and have pulled the model below.
SIMULATOR_BASE_URL=http://<ROG_IP>:11434/v1
SIMULATOR_MODEL=qwen2.5:14b-instruct-q4_K_M
